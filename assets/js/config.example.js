// Copy this file to config.js and use ONLY the public project URL and anon key.
// Never put a service-role key or an admin password in frontend code.
window.APP_CONFIG = Object.freeze({
  SUPABASE_URL: "SUPABASE_URL",
  SUPABASE_ANON_KEY: "SUPABASE_ANON_KEY",
  ADMIN_CENTER_MIGRATION_READY: false
});
